# 赛前检查清单

打印出来带走。每一条都对应着一个**真实发生过的故障模式**，不是通用建议。
赛场没有网，所以这份要落纸。

---

## 出发前（T-1 周）

### 硬件

- [ ] 主板 + **备用板**（一块板子撑不起 4 天）
- [ ] 备用 SD 卡，**已烧好镜像并验证能启动**（只带空卡 = 现场重烧 = 半天没了）
- [ ] 备用 USB 线、备用摄像头排线（排线是最容易松的）
- [ ] 备用电池 / 电源，**按云台的堵转电流选，不是按标称电流**
- [ ] 打印好的靶标集（多种光照、多种距离各一套）
- [ ] 打印本清单 + 引脚表 + 串口协议表
- [ ] 纸质笔记本

### 软件

- [ ] `cv_lite` 在你要带走的那张卡上可用 —— 在 IDE 里 `import cv_lite` 试一次
      （**33 个文件依赖它**，包括 2025 高精版主程序）
- [ ] 所有 `.kmodel` / `.bin` 按 [04_官方资源.md](04_official_resources.md) 核对齐全，
      **并逐个跑一遍确认**（不是"文件在就行"）
- [ ] 每个 kmodel 旁边都有 `deploy_config.json`
      （⚠️ 仓库的 `.gitignore` 会把它排除掉，从 git 克隆出来的一律缺）
- [ ] 单图检测脚本要的 `test.jpg` 已放好 —— 注意
      `det_image_1_2_2.py` 读 `/sdcard/mp_deployment_source/test.jpg`，
      而 `det_image_1_3.py` 读 `/sdcard/test.jpg`，**两个路径不一样**
- [ ] MCU 固件已烧，**且它的串口帧格式和你当天要跑的 K230 脚本逐字节对得上**
      （仓库里有 4 套互不兼容的格式，见[已知问题 P3-1](02_known_issues.md)）
- [ ] **至少三种光照下的阈值已抄在纸上**，不只是存在代码里
- [ ] [已知问题清单](02_known_issues.md)里 P0 级的都改过了，且改过的都上过板

### 已知会咬人的点（确认你处理过了）

- [ ] `07_contest/2024_E_tic_tac_toe/main_tic_tac_toe.py` 若要用，**必须补 `import os`**
      （否则第一次进主循环就 NameError，这个文件目前根本跑不起来）
- [ ] 你带的 `07_contest/2025_E_self_aiming/result/` 主程序，**先确认它跑得到 5 分钟不退出**
      （三个文件各有一到三处必崩点，其中一个还缺 `cv_lite`）
- [ ] 触摸调参 UI 的滑块初值和真实阈值一致 —— 否则一进 UI 按保存就会**覆盖掉你标好的阈值**
      （[已知问题 P1-2](02_known_issues.md)）
- [ ] 要发负值的场合（电机速度），打包用的是有符号 `'h'` 或偏移编码，不是无符号 `'H'`
      （[已知问题 P2-7](02_known_issues.md)）
- [ ] `05_cv_lite/` 的示例若要现场反复跑，先补 `os.exitpoint()` 和 `MediaManager.deinit()`
      （否则每中断一次就要拔电源一次）

---

## 到场第一小时

- [ ] 在**赛场真实光照**下跑一遍完整链路，不是实验室
- [ ] **用触摸 UI 重新标定阈值。永远不要带着默认值上场。**
      这是根 README 自己写的警告，也是 2025 那道题的全部教训
- [ ] 标定完**立刻把数值抄到纸上** —— 保存的值只写进内存里的 list，
      **重启就没了**，不落盘
- [ ] 对着赛场真实的干扰物，确认嵌套框/多条件判别能锁真目标、拒干扰物
      （⚠️ `main_multi_constraint.py` 那条代码路径在本仓库历史上从未执行过 ——
      你是第一个跑它的人，预期它会变慢）
- [ ] 和**真 MCU** 端到端验串口，不是回环
- [ ] 确认激光（GPIO48）在正确的状态跳变上触发 —— 既不会该响不响，也不会乱响
- [ ] 确认按停止按钮之后不用断电就能再跑
- [ ] **演示一次"用默认阈值会失败"** —— 如果不会失败，说明你的标定没起作用

---

## 比赛中

- [ ] 每次保存阈值后**马上抄下来**。回填进 `rect_binary_default/_small/_large` 再拷卡才算固化
- [ ] 程序静默退出时，**第一件事是加 `sys.print_exception(e)`**，在改任何逻辑之前
      （这份代码里三个崩溃点就是这样被藏了一整年）
- [ ] 光照一变就重新标定：云、太阳角度、场馆灯开关、以及**你把板子挪了位置**
- [ ] 删掉调试 print 之后**重新测一次帧率**（每帧 print + 整帧 hex dump 能把帧率砍一半以上）
- [ ] 换主程序版本时重新调阈值 —— 三个主程序的默认阈值**各不相同**，
      不是换个开关那么简单
- [ ] **视觉只是一个子系统。** 结构、电源、MCU 联调都要占时间。
      别把第三天全花在调一个阈值上
- [ ] 每 2 小时把当前能跑的版本连同阈值数值备份一次（U 盘 + 手机各一份）

### 现场调试的正确顺序

```
不工作了
  ↓
1. 光照变了吗？        → 重新标定阈值（90% 的"突然失灵"是这个）
  ↓ 不是
2. 程序还在跑吗？      → 加 sys.print_exception 看 traceback，别猜
  ↓ 在跑但结果不对
3. 串口还对齐吗？      → 丢一个字节就永久错位且不自愈；重启两端最快
  ↓ 数据对
4. 帧率掉了吗？        → 删 print / gc.collect，再看 cv_lite 还是 find_blobs 慢
  ↓ 都正常
5. 才是算法/阈值本身     → 这时才改逻辑
```

---

## 备件包

第二块 K230 · 第二张已烧好的 SD 卡 · 第二个摄像头模组 · USB 转串口模块 ·
万用表 · 杜邦线 · 胶带和扎带 · 小螺丝刀 · 打印靶标集 · 打印引脚图 ·
打印串口协议表 · **纸质笔记本**

---

## 赛后

把这次的实测数据写回仓库：哪个阈值、什么光照、多少帧率、命中如何。
`07_contest/2025_E_self_aiming/result/README.md` 里留了"上板验证结果"的位置。

**这份清单里每一条警告，都来自一次实际踩坑。** 新增了坑就来加一条。
